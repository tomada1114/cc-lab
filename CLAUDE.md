# CLAUDE.md — cc-lab

Claude Code / AI 駆動開発まわりの**検証用サンドボックス**。プロダクションコードではない。

## このリポジトリの前提（Claude Code 向け）

- **Claude Code は確認なしで自由に使ってよい。** ブランチ作成・コミット・push・PR 作成・PR マージ・Issue 作成/クローズ・ブランチ削除まで、都度の許可を取らずに実行してよい
- **意図的に粗のあるコードを含む。** レビューツールの比較検証などのため、わざと欠陥（バグ・アンチパターン）を仕込んだブランチや PR が存在する。ここのコードを他プロジェクトの実装参考にしない
- 検証目的なら履歴の書き換え・force push・大量削除も実行してよい。ただし **リポジトリそのものの削除だけは確認する**
- 検証で作った Issue / PR は消さずに残す。あとから「何をどう検証したか」を辿る一次資料になる

## ディレクトリ

| 場所 | 中身 |
|---|---|
| `src/` | 題材アプリ（TypeScript + Hono のタスク管理 API）。検証の当て木であって、それ自体が成果物ではない |
| `experiments/` | 検証ごとの記録。仕込んだ欠陥の正解リスト、レビュー結果の生ログ、集計 |

## 検証の作法

- **1検証 = 1ブランチ + 1PR。** PR 本文に検証の目的を書く
- 「正解」があるタイプの検証（欠陥の検出率を測るなど）は、正解リストを `experiments/<検証名>/ground-truth.md` に置く。**レビューを走らせる側のコンテキストには入れない**（正解を知っている状態でレビューさせると検証にならない）
- レビュアーごとの生出力は加工せず `experiments/<検証名>/raw/` に残す。集計は別ファイルに分ける
- 条件を比較する検証では、**入力（diff）と指示の粒度を全条件で揃える**。片方だけに追加のヒントを与えない
